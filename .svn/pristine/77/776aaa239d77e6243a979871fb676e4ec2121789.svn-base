package com.ztesoft.mobile.v2.util;

import java.util.UUID;

public class PrimaryKey {
	/**
	 * 生成UUID字符串，去掉所有'-'字符，并转换为大写
	 * 
	 * @return
	 */
	public static String getUuid() {
		return UUID.randomUUID().toString().replaceAll("-", "").toUpperCase();
	}

}
